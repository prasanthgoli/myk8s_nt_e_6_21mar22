apiVersion: v1
kind: ReplicationController
metadata:
 name: mynignx-rc
spec:
  replicas: 2
  selector:
    app: mynignx
  template:
    metadata:
      labels:
        app: mynignx  
    spec:
      containers:
      - name:  nginx-container
        image: nginx
        ports:
        - containerPort: 80